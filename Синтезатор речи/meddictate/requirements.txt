# МедДиктовка — зависимости.
# Установка:  pip install -r requirements.txt
#
# Qt-интерфейс берётся из PySide6: это официальная привязка Qt с лицензией
# LGPL, поэтому собранный exe можно свободно передавать коллегам.
# Если PySide6 недоступен, программа сама возьмёт PyQt6 (лицензия GPL —
# для внутреннего использования либо с коммерческой лицензией Qt).

vosk==0.3.45           # движок распознавания речи (офлайн, русская модель)
sounddevice==0.5.6     # микрофон (PortAudio внутри колеса, драйверы не нужны)
PySide6>=6.6,<6.12     # окно, трей, настройки
pillow>=10.0           # иконка (нужна только при сборке)
